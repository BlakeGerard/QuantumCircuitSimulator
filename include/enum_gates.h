namespace Gates {
        enum Gate {
                H,      // Hadamard
                X,      // Used for X, CNOT, and CCNOT
                Y,      // Used for Y, CY
                Z,      // Used for Z, CZ
                S,      // S gate
                T,      // T gate
                SWAP,   // Swap gate
        };
}